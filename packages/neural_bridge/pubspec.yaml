name: neural_bridge
description: A neural bridge SDK for Flutter applications. Provides pluggable Embedding, LLM, and Conversation management layers with automatic fallback and zero coupling.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.10.0
  collection: ^1.18.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.24.0
  mocktail: ^1.0.0
